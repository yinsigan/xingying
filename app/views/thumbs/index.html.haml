= render partial: "material/tab"
.row
  .col-md-12.clearfix
    .pull-right
      = form_tag upload_public_account_thumbs_path(@public_account, {thumb_group_id: @find_thumb_group ? @find_thumb_group.id : nil}), method: "post", id: "picture_upload" do
        = file_field_tag "image", :class => "filestyle", "data-buttonText" => "#{t('upload')}", "data-input" => false, "data-badge" => false, "data-buttonBefore" => false, "data-buttonName" => "btn-primary", "data-redirect" => public_account_thumbs_url(@public_account), id: "upload"
    .pull-right.marginright20
      %p.help-block
        上传大小: 不超过5M,    格式: png, jpeg, jpg, gif
      = render "shared/progress"
.row.margintop20
  .col-md-12.clearfix
    .tabbable-panel
      .tabbable-line.tabs-below
        %ul.nav.nav-tabs#group{role: "tablist"}
          %li{class: current_page?(public_account_thumbs_path(@public_account)) ? "active" : nil}
            = link_to public_account_thumbs_path(@public_account), type: "button" do
              = t('.no_group')
              %em.num
                (#{@no_group_count})
          = render partial: "thumb_groups/thumb_group", collection: @thumb_groups unless @thumb_groups.empty?

    .btn-group.btn-group-sm.margintop20.pull-right
      = link_to t('.create_group'), new_public_account_thumb_group_path(@public_account), class: "btn btn-default", remote: true
      - if @find_thumb_group.present?
        = link_to t('.delete_group'), delete_public_account_thumb_group_path(@public_account, @find_thumb_group), class: "btn btn-default", remote: true
        = link_to t('.rename_group'), edit_public_account_thumb_group_path(@public_account, @find_thumb_group), class: "btn btn-default", remote: true
- if @thumbs.empty?
  .bs-callout.bs-callout-info
    %h4= t('.no_content')
- else
  .panel.panel-default.margintop30
    .panel-heading
      %h3.panel-title
        = t('.thumb_count', count: @public_account.thumbs_count)
    .panel-body
      .row.operate
        .col-md-12
          #operate-button.normal-header.page-header
            %input.icheck-me#check_all{"data-color" => "blue", "data-skin" => "square", :type => "checkbox"}
            %label{for: "check_all", style: "font-weight: normal"}
              = t('select_all')
            = link_to t('.move_to_group'), move_public_account_thumbs_path(@public_account), class: "btn btn-default disabled", remote: true
            = link_to t('delete'), delete_all_public_account_thumbs_path(@public_account), class: "btn btn-danger disabled", remote: true
      .row
        #pic_content.col-md-12
          = render @thumbs
      .row
        .col-md-12.text-right
          = paginate @thumbs
= render "shared/blueimp_gallery"
