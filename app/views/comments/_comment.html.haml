- cache comment do
  .media.clearfix.padding15.border-top
    %a.media-left.pull-left
      = image_tag comment.user.avatar_url(:normal).to_s
    .media-body.clearfix
      %small.pull-right.text-muted
        = I18n.localize(comment.created_at, :format => :long)
      %h4.media-heading= comment.user.email
      = comment.body
