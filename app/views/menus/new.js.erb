var modal = $("#modal");
modal.modal({
  keyboard: true,
  backdrop: false
});
$("#modal-content").html("<%= j(render 'form', parent_menu: @parent_menu, title: @new_title, name_maximum: @name_maximum) %>");